# Prompt

Keep this recursive or iterative work bounded. Stop at max depth, max iterations, or the exit criterion.
Focus on material issues, not exhaustive commentary.

Return: current depth or iteration, stopping reason, final state.

---
depth: {depth}
iterations: {iterations}
exit: {exit}
