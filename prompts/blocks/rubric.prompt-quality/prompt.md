# Prompt

Evaluate this prompt against the quality checklist.

For each criterion below, answer yes, partial, or no — and state the evidence in one sentence:

- Is the task specific?
- Is the output shape explicit?
- Are constraints and non-goals visible?
- Is the model likely to misread the request?
- Is there enough context to solve it without guessing?
- Would a cleaner version of this prompt be easier to use?

Then give:
- overall verdict: ready / needs revision
- the single highest-leverage fix if revision is needed

Prompt to evaluate:
{paste prompt}
