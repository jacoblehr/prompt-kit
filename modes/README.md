# Modes

Modes are reusable reasoning-state blocks.

They are not tones, personalities, or writing styles.

Use a mode to control how the model thinks about the task:

- `explore` for breadth
- `decide` for narrowing
- `critique` for pressure-testing
- `reflect` for learning

In manual use, a mode block is often the first thing you choose.
